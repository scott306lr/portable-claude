# Global memory (all projects)

<!--
This file is symlinked to ~/.claude/CLAUDE.md on every machine you run
./install.sh on. Put your working preferences here: how you want Claude
to code, communicate, and verify its work. It loads in every session.
Replace the example below with your own rules.
-->

## Verify before claiming done

When you call a task complete, actually run it — the tests, the script,
the build — and show the real output. "Done" means observed-working,
not should-work.
